<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mensajeriaprogra">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activites.ChatMultiActivity"></activity>
        <activity android:name=".activites.ConfirmMultiChatActivity" />
        <activity
            android:name=".activites.AddMultiUserActivity"
            android:parentActivityName=".activites.HomeActivity" />
        <activity
            android:name=".activites.ConfirmImageSendActivity"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity android:name=".activites.ChatActivity" />
        <activity
            android:name=".activites.ProfileActivity"
            android:parentActivityName=".activites.HomeActivity" />
        <activity android:name=".activites.HomeActivity" />
        <activity android:name=".activites.CompleteInfoActivity" />
        <activity android:name=".activites.CodeVerificationActivity" />
        <activity android:name=".activites.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.MyFirebaseMessagingClient"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />

        <receiver
            android:name=".receivers.ResponseReceiver"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>